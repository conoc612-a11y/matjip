# TODO — matjip 미결 과제

작업이 **막혀서 보류된 것**과 **사용자 확인·외부 문의가 필요한 것**을 모아 둔다.
"기록만 남기고 안 한 것"이 `HANDOFF.md` 세션 기록 속에 흩어져 묻히는 걸 막으려고 만들었다(2026-08-21).

**규칙**
- 항목을 끝내면 지우지 말고 `## ✅ 완료` 로 옮기고 **완료일·커밋**을 적는다(같은 조사 반복 방지).
- 각 항목엔 **다음에 할 일 한 줄**과 **근거 문서 위치**를 반드시 적는다.
- 조사가 이미 끝난 건 여기 요약만 두고 상세는 `TROUBLESHOOTING.md` 를 가리킨다.

---

## 🔴 0-A. 경매 목록 수집이 지금 실패한다 — 요청량 스로틀링 의심 (2026-08-22)

**상태**: `auction.json` 이 **2026-08-17 에서 멈춰 있다.** 8/22 로컬 실행이 실패했다.

- 2026-08-22 15:28 로컬 실행: 서울중앙~7번째 법원까지 **2,148건 정상 수집** 후
  "화면 로드 실패 → 재시도" 반복 → `collect_auction.js:324` 에서 크래시.
- 직후 curl 로 같은 URL 을 치면 **200/0.2초로 멀쩡**하다 → HTML 은 살아 있고
  **SPA 초기화**(법원 셀렉트 미등장)에서 막힌다.
- 그날 요청량이 과했다: 수집기 2개 동시 실행 사고 + 감정평가서 조사 반복 + 사건별 수집.
  이 사이트는 "폭주 시 차단"을 공지하고 있다(`collect_auction.js` 헤더).

**다음에 할 일**: **몇 시간~하루 쉬었다가** matjip 의 `⟳ 데이터 갱신` → "빠른 갱신(목록만)".
⚠️ 실패했다고 즉시 재시도하지 말 것 — 상황을 악화시킨다.
성공하면 `auction.json`·`auction_sched.json` 을 커밋해야 사이트에 반영된다(자동 커밋 버튼 사용 가능).

📖 근거: `TROUBLESHOOTING.md` §48

---

## 🟡 0-B. `collect-auction.yml` 되살렸다 — cron 켤지는 아직 미확정

**상태**: 워크플로 **부활 완료**(수동 실행 전용). 원인도 확정됐다.

§48 로 확정: **해외 IP 차단이 아니다.** Actions 러너에서 법원 사이트 HTTP 200/0.74초.
진짜 원인은 **SPA 초기화 대기 설정** 두 곳이었고 `collect_auction.js` 에서 고쳤다:
- `page.goto` 의 `waitUntil: 'domcontentloaded'` → **`'commit'`** (해외에서 전자는 30초 타임아웃, 후자는 1,230ms)
- 법원 셀렉트 폴링 **15초 → 60초** (진단 실측 등장 시각 **17,892ms** — 3초 부족해서 매일 실패)

진단 워크플로 2개는 사용 후 삭제했다.

**남은 판단**: `schedule` cron 은 **아직 주석 처리 상태**다(`.github/workflows/collect-auction.yml:31`).
수동 실행이 **몇 번 연속 성공하는지 확인한 뒤에** 풀 것.

**진행 중**: run `32559244779` (2026-08-22 07:18 UTC 시작). 셋업(npm ci·playwright) 전부 통과하고
수집 스텝에서 50분 이상 진행 중 — 예전에 죽던 지점은 넘겼다. `timeout-minutes: 180` 안에 끝나는지 확인 필요.

**다음에 할 일**: `gh run view 32559244779` 로 결과 확인 → 성공이면 몇 번 더 수동 실행 → cron 주석 해제.

📖 근거: `TROUBLESHOOTING.md` §48, 워크플로 파일 상단 주석

---

## 🔴 1. 감정평가서 PDF 를 앱에서 보게 하기 — 외부(협회) 차단으로 보류

**상태**: 조사 **완결**. 우리 코드로는 불가. 협회 뷰어가 **IE 플러그인(ActiveX) 전용**이라
현대 브라우저에선 문서가 아예 렌더되지 않는다. 사용자 실제 Chrome + 아크로뱃 설치로도 빈 화면.

**📖 상세: [`TROUBLESHOOTING.md` §47](TROUBLESHOOTING.md)** — URL 조립법, API 필드 매핑,
시도했다 실패한 것 전부(다시 하지 말 것), 현대화 시 실행할 구현안까지 다 적혀 있다.
**다시 조사하지 말 것.**

### 다음에 할 일 (2026-08-25 주)

1. **한국감정평가사협회 ☎ 02-3465-9870** (법원경매정보 FAQ 가 안내한 번호) 에 문의:
   - ① 감정평가서 뷰어가 Chrome/Edge 에서 동작하지 않는데 **현대화(PDF 직접 서빙) 계획이 있는지**
   - ② 있다면 **시점**
   - ③ `ca.kapanet.or.kr/view/...` URL 을 **외부 서비스에서 링크로 참조해도 되는지**
     (FAQ 에 "협회 동의하에 링크"라고 적혀 있어 동의 범위를 확인해야 한다)
2. **법원경매정보 고객센터**: 감정평가서 외 문서(현황조사서·매각물건명세서)는 법원 제공이라고
   FAQ 에 적혀 있다 → 그건 어떤 방식이며 표준 URL 이 있는지.
3. 답변 내용을 이 항목에 적어두고, 현대화 예정이면 §47-6 "현대화되면 할 일"을 실행.

### 현대화되면 할 일 (요약 — 상세는 §47-6)

- 수집기에 "사건내역 화면의 **보이는** `input[value="감정평가서"]` 클릭 → `selectAeeWevlInfo.on`
  응답 캡처" 한 단계 추가. **당사자내역 캡처와 같은 페이지 방문에서** 처리하면 사건당 2~3초만 늘어난다.
- URL 은 `https://ca.kapanet.or.kr/view/{cortOfcCd에서 B제거}/{csNo}/{ordTsCnt}/{aeeWevlNo}/{wrtYmd}`
- ⚠️ **URL 유효성을 HTTP 200 으로 판정하지 말 것** — 엉터리 값도 200/1,016B 를 준다(§47-4).

### 지금 당장 가능한 부분 (PDF 와 무관하게 가치 있음)

조사 중 발견한, **현재 화면에 없는** 감정평가 정보. 위 수집 단계 하나로 같이 확보된다:
`dspslPrcCrtrYmd`(**가격시점** — 감정가가 언제 기준인지, 투자 판단에 직접 쓰임) ·
`exmnYmd`(조사일) · `wrtYmd`(작성일) · `aeeEvlExamrNm`(감정 담당자) · `aeeWevlNo`(평가서번호)

---

## 🟡 2. 당사자·감정평가 정보 보강 — 통합 수집기로 채우는 중 (진행률 낮음)

**코드는 완료**(당사자 `7ca0999` · 감정정보 `e10e41a` · 통합 수집기 `e4cbd00`).
**남은 건 시간을 들여 돌리는 것뿐이다.**

### 현재 커버리지 (2026-08-22, 고유 사건 2,756)

| 항목 | 보유 | 비고 |
|---|---|---|
| 사진 | 2,534 | 미보유 222 는 대부분 수집 불가(3번 항목) |
| 상세 | 2,602 | 미보유 154 도 같음 |
| **당사자** | **33** | ← 거의 안 채워짐 |
| **감정평가 정보** | **1** | ← 기능은 되는데 데이터가 없음 |

### 어떻게 채우나

matjip 의 `⟳ 데이터 갱신` → **"보통 갱신 (목록 + 사건 500건)"** 을 누른다(회당 약 3시간).
또는 직접: `node tools/update_auction.js --max 500`
- 돌릴 때마다 남은 것만 줄어든다. 다 채워지면 "할 일 없음"이 된다.
- 중간에 끊겨도 안전하다(사건마다 즉시 저장).
- **약 6회(총 16시간)** 면 완료. ⚠️ 한 번에 16시간을 잡지 말 것.
- 실측 **건당 22.5초**(앞서 "6시간"이라 적었던 것은 틀렸다).

### 왜 "새 사건만 쌓이게 두고 방치"로는 안 되나
현재 목록의 **79%가 유찰 사건(2,723/3,442)** 이고 몇 달씩 목록에 남는다. 자연 교체로
해결되지 않는다(신건은 719건뿐). 그래서 한 번은 돌려서 메꿔야 한다.

### 대상 조건에 관한 주의
`needPhotos || needDetail || needParties || needAee` 중 하나라도 해당하면 대상이 된다.
**새 필드를 추가하면 `collect_auction_case.js` 의 이 조건에 한 줄을 반드시 늘려야 한다** —
안 늘리면 기존 사건에서 그 필드가 **영구히 비어 있게 된다**(당사자·감정정보에서 실제로 겪음).

---

## 🟡 3. 미보유 사진 222·상세 154 — **원인 규명 완료. 대부분 법원이 막은 사건**

**2026-08-21 사용자 Chrome 으로 법원 사이트에 직접 접속해 확인.** 우리 버그가 아니었다.

`물건상세조회` 버튼이 **화면에 보이지만 `disabled`** 다(`w2trigger_disabled`).
법원이 상세 제공을 막은 상태로 **기다려도 열리지 않는다.**
실측 사건: `2025타경102901`, `2021타경100523` — **둘 다 미종국인데도** 비활성이었다.

- 예전 로그가 disabled 든 아예 없든 전부 `"물건상세조회 버튼 없음(종결/취하?)"` 로 찍혀서
  원인을 오판하게 만들었다 → 상태별 구분 로깅으로 수정(커밋 `bb3ca51`).
- 이어붙은 사건번호 파싱 버그로 실패했던 **6건**은 수정으로 살아났다(커밋 `19c73d5`).
  📉 앞서 "36건이 살아난다"고 한 것은 틀렸다 — 30건은 공백 구분이라 원래도 잘 파싱됐다.
- **남은 대부분은 수집 불가가 맞다.** "3시간 돌려 0건"은 수집기가 제대로 판단한 결과였다.

**그래도 건지는 게 있다**: 통합 수집기(`collect_auction_case.js`)는 disabled 사건에서도
**당사자내역만이라도 보강**한다 — 상세(pgj15B)는 못 받아도 검색 결과(pgj15A)는 오기 때문이다.
실측: `2023타경110870(중복)` 에서 당사자 **24건** 확보.

**다음에 할 일**: 없음. 법원이 상태를 풀면 평소 수집에서 자동으로 들어온다.

### ⛔ 다시 하지 말 것
- "고정 2초 대기가 짧아서 놓친다" 가설 → **틀렸다.** 15초 폴링을 넣었더니 disabled 사건마다
  15초를 헛되게 버렸다(철회). 측정했던 11.5초는 버튼의 **존재** 시점이고 disabled 여부는 안 본 값이었다.
- 사용자 Chrome 에서 버튼 개수만 세어 "있으니 우리 버그" 로 결론내기 → **`disabled` 를 함께 확인할 것.**

---

## 🟢 3-B. 세입자(상호) 리스트 — **API 승인 완료. 구현만 남았다**

**상태**: 2026-08-22 사용자가 `15012005` 활용신청 → **승인 확인**. 실제 호출까지 검증 끝났다.

**검증된 것 (다시 조사 불필요)**:
```
GET https://apis.data.go.kr/B553077/api/open/sdsc2/storeListInPnu?serviceKey=<DGK>&key=<PNU>
→ 봉천동 34-4 (PNU 1162010100100340004) = 파리바게뜨 건물
   -층 파리바케트 행운점 [빵/도넛] / 2층 JDS헤어디자인 [미용실] / 3층 제이엠뷰티룸 [피부 관리실]
```
- 파라미터명은 **`key`** (pnu·lnoCd·cd 는 NO_MANDATORY_REQUEST_PARAMETERS_ERROR)
- 팝업이 이미 갖고 있는 **PNU 를 그대로** 넘기면 된다(`pnuParts` 가 쓰는 값)
- 응답 39필드. `flrNo`(층) 채움 **78%**, `hoNo`/`dongNo` **0%** → **호 단위 표시는 포기할 것**

**다음에 할 일 (구현)**:
1. Edge Function 프록시 신설(`sbiz-proxy` 등) — **serviceKey 를 프론트에 두지 말 것**
2. `fillLandInfo` 가 건축물대장을 채우는 자리 옆에 붙인다(PNU 가 이미 그 스코프에 있다)
3. 갱신이 **분기**라 폐업이 최대 3개월 늦다 → "○○년 ○분기 기준" 표기 필요
4. ⚠️ `DGK` 는 Encoding 키다. `curl --data-urlencode` 로 넘기면 이중 인코딩되어 실패한다

📖 근거: `TROUBLESHOOTING.md` §50(DB 6종 비교), §51-4(실측 응답·채움률)

---

## 🟡 4. 기존 사건에 사진이 추가되면 영구히 못 받는 문제

**상태**: 미해결. 다만 통합 수집기에는 `--force` 가 있어 개별 강제 재수집은 가능해졌다.

대상 선정이 `!photosDb[cn]` — **사건번호 기준으로 이미 있으면 건너뛴다.**
→ 법원이 **기존 사건에 사진을 추가**하면(유찰 후 재감정 등) 그 사건은 새 사진을 못 받는다.

**다음에 할 일**: `collect_auction_case.js` 에서 저장된 사진 장수(`photosDb[cn].length`)를
응답의 `csPicLst` 길이와 비교해 **달라졌을 때만** 다시 저장하는 방식 검토.
통합 수집기는 어차피 같은 응답을 이미 받아 두므로 **추가 요청 없이** 비교할 수 있다
(기존 분리 구조에서는 불가능했던 것 — 통합의 부수 이득).

---

## 🟡 5. 사진 메타 stale 310건 정리

`auction.json` 은 매일 전량 교체되므로 취소·취하·매각완료 사건은 목록에서 사라지는데,
`auction_photos.json` 에는 그 사건의 사진 메타가 남는다(현재 **310건**). R2 오브젝트도 함께 남는다.

**다음에 할 일**: 목록에 없는 `cn` 을 메타에서 제거 + R2 오브젝트 삭제할지 결정.
보류 이유 — 사건이 일시적으로 목록에서 빠졌다 돌아오는 경우가 있어 즉시 삭제가 위험하다.
(안전한 방식: "N일 연속 목록에 없으면 삭제" 같은 유예 규칙)

---

## 🟢 6. 푸터 위젯 Supabase 호출 캐싱 (환율 제외)

**상태**: 청약공고만 24시간 캐시 적용 완료(커밋 `1803216`). 나머지는 미적용.

- 날씨(`kma-weather-proxy` 0.5초) · 방문자수 · 회원수 는 페이지 로드마다 호출.
- **환율은 의도적으로 캐시하지 않음**(실시간 변동 — 사용자 판단).
- 체감 영향이 가장 작은 항목이다(병렬이고 지도 렌더를 막지 않음).

**다음에 할 일**: 날씨 정도만 10~15분 캐시 검토. 우선순위 낮음.

---

## 🟢 7. `pre-r2-migration-backup` 브랜치 삭제

R2 이전 전 상태를 보존한 백업 브랜치. **이 브랜치가 있는 동안 GitHub 저장소 용량이 줄지 않는다**
(사진 blob 을 참조). 1~2주 안정 확인 후 삭제 예정이었다(2026-08-20 생성).

**다음에 할 일**: `git push origin --delete pre-r2-migration-backup` (2026-09-03 이후 권장)

---

## ✅ 완료

- **통합 수집기 collect_auction_case.js** — 2026-08-21, 커밋 e4cbd00.
  사진·상세가 같은 응답(pgj15B)을 쓰는데 따로 방문하던 중복을 제거. 전량 32h→16h, 요청 절반.
  기존 두 스크립트는 되돌릴 수 있게 보존. disabled 사건에서도 당사자만 보강한다.
- **병합·중복 사건번호 파싱 수정** — 2026-08-21, 커밋 19c73d5. 6건 정정, 정상 2,570건 회귀 없음.
- **disabled 버튼 오진 로그 수정** — 2026-08-21, 커밋 bb3ca51.
  "버튼 없음(종결/취하?)" 로 뭉뚱그리던 것을 ready/disabled/없음 으로 구분. 원인 오판을 막는다.

- **매각예정 물건(`--sched`) 수집 살리기** — 2026-08-21, 커밋 `fba3b4f`.
  법원이 화면을 둘로 나눠 놨는데(진행=2주 / 예정=2개월) 예정 쪽을 한 번도 수집하지 않아
  `auction_sched.json` 이 저장소에 없었다. 워크플로에 `--sched` 단계 추가 → 매일 자동.
- **경매 좌패널 감정평가액·최저매각가격 표시** — 2026-08-21, 커밋 `4a19ee5`.
  `auction.json` 에 이 값이 3,442행 중 2행/1행뿐이라 비어 있었고, 상세(`dspsl`)에는 전건 있어 폴백.
- **기일내역 금액이 좁은 패널에서 잘리던 문제** — 2026-08-21, 커밋 `4a19ee5`.
  데이터는 정상(매각기일 9,987/9,987 에 금액 있음)이고 flex 레이아웃 오버플로가 원인.
- **매각예정 레이어 + 데이터 2,955건** — 2026-08-21, 커밋 `0abbdda`.
  예정 매각기일 2026.09.07~10.20, 좌표 98%. 진행 목록과 겹침 5건뿐(=거의 전부 신규 발굴).
- **금액 단위 버그(만원→억 변환 누락)** — 2026-08-21, 커밋 `0abbdda`.
  `collect_auction.js` 의 `eok()` 가 원을 /10000 해 **만원**으로 저장하는데(이름이 eok 라 헷갈림)
  `wonEok()` 은 억으로 취급 → 2.81억이 "28,100억원" 으로 표시. `manwonToEok()` 추가로 해결.
  진행 물건은 appr 이 3,442행 중 2행뿐이라 이 버그가 가려져 있었다.

- **Cloudflare Worker 로 사진 서빙 (채택 안 함)** — 2026-08-22.
  r2.dev 의 LAX 우회를 Worker 로 없앨 수 있다는 가설로 만들어 배포까지 했으나 **실측에서 기각**.
  Worker 도 CF-RAY 가 **LAX** 였고(플랜에 묶인 라우팅), r2.dev 는 이미 immutable 캐시를 보내고 있어
  사진 **첫 로드가 오히려 느렸다**(0.96~1.37초 vs 0.89~1.28초). `land.html` 은 변경하지 않았다.
  Worker 는 무료라 배포 상태로 남겨 두고, 재검토 조건(유료 플랜/다중 사용자/커스텀 도메인)만 적어 뒀다.
  부수 효과: 계정 workers.dev 서브도메인을 **`matjip-kr`** 로 등록했고 `wrangler login` 도 완료됐다.
  📖 `TROUBLESHOOTING.md` §49
- **팝업에 상호 자동 표시** — 2026-08-22, 커밋 `2a3ff73`.
  건물명이 없을 때만 카카오 장소 조회를 자동 실행(있으면 8회 호출만 늘고 얻는 게 없어 안 함).
  결과 캐시 추가. 배포본 실측: 용도 `단독주택` 클릭 → `GS25 봉천래인점 편의점 · 6m` 자동 표시.
- **벡터지도 전환 검토(호버 인식)** — 2026-08-22, **경로 없음으로 결론**.
  V-World `.pbf` 는 7개 레이어 전부 HTTP 500(같은 키로 래스터는 200), 공식 2D Data API 158종에도
  벡터타일 없음. 네이버·카카오 SDK 도 기본지도 POI 라벨을 객체로 노출하지 않는다.
  📖 `TROUBLESHOOTING.md` §50-6

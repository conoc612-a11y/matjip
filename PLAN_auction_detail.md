# 경매 상세 패널 계획 (disco.re 벤치마크)

> 작성 2026-08-13. 목적: 사용자 요구 "왼쪽 패널 시각적 인식 개선 + 경매 물건 2번째 패널 페이지(사진 상단 + 사건번호/주소/감정가/최저가/매각기일)"를 구현하기 전, 현황·갭·설계·리스크를 정리해 결정을 받는다.

## 0. 결론

- **가능하다. 그리고 경매 기능의 약 80%는 이미 구현되어 있다.** (HANDOFF 2026-08-09 (15)·(16), 커밋 `0a18d49`·`518d534`·`50d84e6` 배포 완료)
- 빠진 것은 ① 왼쪽 패널의 "시각적 인식" 개선 ② 물건 사진 ③ 2번째 패널(상세 페이지) 3가지.
- **핵심 리스크 = 물건 사진 수집.** courtauction.go.kr은 SPA(직접 URL 불가, 클릭 전용)이고 IP 차단 위험이 있어 수집 범위를 실측으로 확정해야 한다.

## 1. 현황 (근거: 파일·실측)

### 데이터 — `auction.json` (이미 있음, 926.9KB)
- 수집기: `tools/collect_auction.js` (playwright-core + system Chrome, courtauction.go.kr PGJ151 화면 클릭 수집)
- 규모: **서울+경기 13개 법원, 진행중 물건 2,949건** (좌표 확보 2,896건, 98.2%)
- 필드 (실측): `court / cn / no / addr / note / appr / dept / sale / use / low / status / lat / lng` — `appr`(감정평가액)·`low`(최저매각가격)는 **억 단위**, `sale` = 매각기일("2026.08.20"), `status` = 진행상태("유찰 2회" 등)
- 참고: land.html:1583 주석 "만원 단위"는 **낡은 주석** — 실제는 억 단위(HANDOFF (16) 실측·수정 완료). 건드릴 때 같이 정리.

### UI — land.html (이미 있음)
- 레이어: `auctionCluster` (markerClusterGroup), 마커 = ⚖️ divIcon, 상태색 유찰 `#fa5252`/그 외 `#f08c00` (land.html:1584~1631)
- 왼쪽 사이드 패널 `#auction-panel` (land.html:1640~1724): 사건 목록 `#ap-list` 2,896건. 행 = 상태배지·주소·☆즐겨찾기·사건번호 버튼·법원·물건번호·감정/최저/용도/매각기일
- 행 클릭 = 지도 이동 + 팝업 (`zoomToShowLayer` → `openPopup`), 사건번호 클릭 = 법원 사건검색 새 탭, ☆ = localStorage `land_auction_fav`, 리사이즈 `makeResizable`(200~640px)
- 팝업(auctionPopupHtml, land.html:1609): 법원·사건번호·물건번호·용도·감정평가액·최저매각가격·진행상태·담당계 매각기일·비고

### 제약 (실측 기록: TROUBLESHOOTING §6-11·§6-12)
- courtauction.go.kr: **직접 fetch 불가**("파라미터가 없습니다" + 반복 시 IP 차단 1시간). WebSquare5 클릭 요청만 유효, `GAP_MS=1000` 필수
- **상세 URL 불가**: 그리드에 `a[href]` 없음(사건번호만 확보). 문서(감정평가서·현황조사서)도 상세 화면 버튼 클릭으로만 열림
- 지오코딩 함정: 경매 주소는 `type=ROAD` + 층/호 정제(`cleanAddr`) 필요

## 2. 디스코 대비 갭

| 항목 | disco.re | 현재 matjip land.html |
|---|---|---|
| 왼쪽 목록 패널 시각 | 이미지 포함 카드형, 명확한 헤더 | 텍스트 행 목록 (기능은 갖춤) |
| 물건 상세 | **2번째 패널 페이지**: 사진 상단 + 사건번호/주소/감정가/최저가/매각기일/용도/유찰 | 행 클릭 → 지도 팝업뿐 |
| 물건 사진 | 사진 그리드 | **없음** |
| 단위 토글 | ㎡↔평·총액/단가 | **이미 있음** (`u-price`/`u-area`, 2026-08-13 구현) |

→ 디스코가 갖고 우리가 없는 것 = **상세 패널 페이지 + 사진**. UI는 기존 인프라로 바로 가능, 사진만 데이터/수집 문제.

## 3. 데이터 확장 설계

### 3.1 auction.json v2 — 사진 필드 추가
- 목표: 각 물건에 `photos: string[]` (사진 URL 1~N장)
- 사진 URL은 **물건 상세 화면에서만** 확보 가능(§6-11·§6-12) → collect_auction.js가 상세 클릭 후 이미지 요소 URL 추출
- 상세 URL(`caseUrl`)은 **추가 안 함** — SPA라 존재하지 않음(§6-12 실측). 사건검색 링크(현행) 유지
- **면적 필드 없음** → 경매는 단가(만원/㎡) 계산 불가. `u-price` 토글은 상세 패널의 감정가·최저가 표기에만 연결(총액/단가 변환은 면적 필요하므로 단가 모드 무시 or 보류)

### 3.2 사진 접근 방식 3안 (실측 후 결정)
1. **URL만 저장 + 공식사이트 핫링크**: 사진은 courtauction 서버에서 직접 로드. 저장 부담 0, 단 외부 의존·차단 리스크. (경매알리미가 공식사이트 이미지 핫링크 사용 중 — §6-10 실측)
2. **우리 서버(Supabase Storage)에 저장**: 수집 시 다운로드 후 Storage 업로드, URL 치환. 참조 안정, 단 수집량·비용 증가
3. **사진 포기**: 상세 패널은 사진 없이 정보만, "법원 사이트에서 사진 확인" 유도 (최소 안)

## 4. UI 설계

### 4.1 왼쪽 패널 시각 개선 (1단계, 리스크 낮음)
- `ap-head` 개선: ⚖️ + "법원경매" 타이틀 강조, 카운트 배지("진행 2,896건"), 진행상태 요약
- 행 카드화: 주소를 1줄 강조(크게·bold), 감정가·최저가 **오른쪽 정렬 숫자**, 매각기일 **D-day 배지**(남은 일수 계산), 유찰 배지 구분
- 호버 시 해당 마커 강조(선택 과제)

### 4.2 상세 패널 — 2번째 페이지 (2단계, 핵심)
- 상태 전환: `#ap-list`(목록) ↔ `#ap-detail`(상세), 헤더에 "← 목록" 뒤로가기
- 레이아웃 (디스코 순서 준수):
  1. **사진 상단** (1장 or 좌우 화살표 그리드, 없으면 placeholder)
  2. 헤더: 주소 + 상태 배지 + 유찰
  3. 핵심 가격 카드: **감정가 / 최저가 / 매각기일(+D-day)** — `u-price` 토글 연결
  4. 상세 그리드: 사건번호·법원·물건번호·용도·진행상태·담당계·비고
  5. 액션: ☆즐겨찾기 · "지도에서 보기"(기존 행 클릭 동작) · 법원 사건검색/물건검색 링크
- 구현: land.html 내부, 기존 `auctionRowHtml`/`auctionPopupHtml`와 나란히 `auctionDetailHtml(d)` 추가, 목록 onclick에서 분기

## 5. 단계별 작업 + 성공 기준

| 단계 | 작업 | 성공 기준 |
|---|---|---|
| **0. 실측** (가장 먼저) | collect_auction.js에 "상세 클릭 → 이미지 URL 추출" 실측 코드 추가, **1개 법원 10건만** 실행 | 이미지 URL 패턴 확인 + 직접 접근(핫링크) 여부 판정 |
| **1. UI 뼈대** | 상세 패널(사진 placeholder 포함) + 목록↔상세 전환 + 행 클릭 동작 | 로컬에서 목록 클릭→상세 전환, 정보·D-day 배치 확인 |
| **2. 데이터 수집** | 실측 결과 따라: 전량(2,896건, GAP_MS 유지·수일 분산) or 샘플 → `auction.json v2` | 수집 완료, photos 필드 채워짐, IP 차단 없음 |
| **3. 통합** | land.html 상세 패널에 실제 사진 연결, 낡은 주석(1583) 정리 | 배포 후 실화면에서 사진+정보 확인 |
| **4. (선택)** | 매각예정 `--sched` 병합(HANDOFF (16) 보류 항목) | 예정 물건 표시 |

## 6. 리스크

- **IP 차단**(최대 리스크): 반복 요청 시 1시간 차단 실측(§6-11). 전량 수집은 `GAP_MS=1000`로도 ~50분+, 수일 분산·실패 시 즉시 중단
- **사진 핫링크 정책 미확정**: 공식사이트 정책 변경 시 사진 깨짐. 저장 안 하면 이 리스크 상존
- **상세 페이지 로드 비결정적**(§6-13): PGJ157처럼 재방문 시 빈 화면 가능 → 재시도 로직 필요
- SPA 상세 URL 불가 → "사진만 빼고 상세 정보"로 우회하던 것도 실측 결과에 따라 재판정

## 7. 결정 필요 (계획 확정 전 확인)

1. **사진 수집 범위**: 서울+경기 2,896건 전량 vs 실측 후 샘플 우선
2. **행 클릭 동작**: 상세 패널 열기로 변경(지도 이동은 상세 내 "지도에서 보기"로) vs 기존 지도 이동 유지 + 별도 상세 버튼
3. **사진 접근**: 핫링크(안 1) vs Storage 저장(안 2) vs 포기(안 3) — 단계 0 실측 결과로 판정

---

## 8. 경매 상세 바로가기 — "법원 딥링크 불가" 실측 확정 + auctionmsg식 자체 렌더링 로드맵 (2026-08-17)

> 사용자 요구: "경매 상세에 바로가기 버튼(사건내역·기일내역·문건/송달·감정평가서·현황조사서·매각물건명세서) 클릭 시, 해당 내용이 바로 보여야 한다." auctionmsg.com을 참고하라고 지시.
> 결론: **법원 사이트 딥링크·자체 렌더링 모두 현재 실측상 불가능.** 현재 구현(사건번호 복사 + 사건검색 새 탭) 유지, 나중에 자체 데이터 수집 시 auctionmsg식으로 교체하는 로드맵만 문서화.

### 8.1 딥링크 불가 실측 근거 (2026-08-17)

- **WebSquare5 엔진 소스 확정**: `$W._g.getParameter` 구현 직접 확인 — URL 쿼리스트링의 `param`은 **문자열 그대로** 반환, JSON 자동 파싱 없음. 객체 param은 내부 화면 이동(`setSrc`) 시 `userData` 메모리로만 전달 → **신규 탭 URL 접속 시 사이트가 객체를 받을 수단이 없음**.
- **사건검색 프리필 실측 실패** (헤드리스 v28): `param={"cortOfcCd":"서울중앙지방법원","csNo":"2025타경103327"}`를 URL로 넘겨 사건검색 화면 접속 → 법원은 기본값(첫 항목), **연도 2026(기본)·사건번호 빈칸** = 프리필 안 됨. 사이트 `prevInit()`은 param을 객체로 기대(`param.csNo`)하는데 문자열이 와서 실패.
- **사건상세 딥링크 실측 실패** (v13, v22): `PGJ15AF01`에 param URL 접속 → 화면은 뜨나 `법원:- 사건번호:-` + "잘못된 번호". `$p.url()`도 param을 URL 쿼리스트링으로 펼치면서(한글 EUC-KR 인코딩) **URIError** 발생(v36).
- 사건검색·사건상세 화면에서 "기일내역" 등 6개 항목은 **모두 같은 사건상세 화면 내부 탭/문서** → 사건상세로 URL 진입이 안 되므로 6종이 전부 같은 사건검색 화면으로 열리는 게 구조상 필연.

### 8.2 auctionmsg.com 조사 결과 (2026-08-17 실측) — 법원 연결 아님

- **auctionmsg는 자체 DB 서비스**: 상세 페이지(`/auction/detail/507066`)의 기일내역·사건내역·감정평가서·현황조사서·매각물건명세서는 전부 자체 도메인에서 렌더링. 외부 링크는 네이버 지도·OSM 저작권뿐, `courtauction.go.kr` 링크 **0건**.
- **main.js(1.9MB) 바이트 검색**: `courtauction`, `PGJ`, `w2xPath`, `기일내역` 등 법원 연결 코드 **0건**. Angular SPA + 자체 데이터 API.
- "기일내역 누르면 바로 나오는" 이유 = 자체 DB에 법원 경매 데이터(기일·문건·가격 등)를 복제해 두고 자기 사이트에서 보여주는 것.

### 8.3 현재 구현 (c922b155, 빌드 b38, 배포 완료)

- 6종 `.apd-go` 버튼: 클릭 시 **사건번호 클립보드 자동 복사**("복사됨 ✓" 1.2초) + 사건검색 새 탭(`AUC_SEARCH_URL`, pgjId 포함 실측 검증 URL).
- 이 상태에서 새 탭의 사건검색 창에 **Ctrl+V 한 번**이면 사건상세로 진입 가능 — 딥링크 불가 한계 내 최선의 UX.

### 8.4 향후 로드맵 — auctionmsg식 자체 렌더링 (자체 데이터 수집 시)

1. **데이터 수집 파이프라인**: `tools/collect_auction.js` 확장 — 사건 목록 외에 사건상세 화면의 기일내역(회차·매각기일·최저가·결과), 문건/송달, 감정평가서, 현황조사서, 매각물건명세서 데이터를 상세 클릭으로 수집(`GAP_MS=1000` 유지, IP 차단 주의 §6-11). 상세 URL이 없으므로 **사이트 내 클릭 자동화로만** 수집 가능.
2. **auction.json v3**: 물건별 상세 내역 필드 추가 (`gihui`/`docs`/`appraisal`/`survey` 등).
3. **land.html 상세 패널 재설계**: 6종 버튼을 "새 탭 열기"에서 "패널 내 탭 전환"으로 변경 — 기일내역 버튼 클릭 → 자체 데이터 렌더링 (auctionmsg 방식).
4. **트리거/정책**: 법원 사이트가 IP 차단·구조 변경 시 수집 실패 — 재시도·부분 실패 허용 설계.
5. 성공 기준: 배포 후 기일내역 버튼 클릭 → 새 탭 없이 상세 패널에서 회차별 기일내역이 바로 보임.
